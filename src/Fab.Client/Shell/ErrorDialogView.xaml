<UserControl
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="Fab.Client.Shell.ErrorDialogView"
			 MaxWidth="900" d:DesignWidth="299" d:DesignHeight="186">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TextBox x:Name="Error"
				 TextWrapping="Wrap"
				 IsReadOnly="True"
				 MaxHeight="500"
				 VerticalScrollBarVisibility="Auto" />
		<Button x:Name="Close" Grid.Row="1" HorizontalAlignment="Right" TabIndex="0" Style="{StaticResource ButtonStyle}">
			<TextBlock Grid.Column="1"
						Style="{StaticResource IconButtonTextStyle}"
						Foreground="{Binding BorderBrush, ElementName=Close, FallbackValue=Black}"
						Margin="5" HorizontalAlignment="Left"
						Text="{Binding LocalizedStrings.ErrorDialogView_Close, FallbackValue=Try to continue}" />
		</Button>
	</Grid>
</UserControl>